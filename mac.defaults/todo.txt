
Turn off .DS_Store from being saved

Replace caps lock function with esc key

Replace background with plain colour

Full screen window
map keys Shift+Command+= to maximise current window
to do this manually go to 
System Settings > keyboard > keyboard shortcuts > App Shortcuts > Create new
shortcut (+ button). 
Then map command above to some name. 
Theres a step im missing here (maybe a restart?) because this didnt work for

Automatically remove apps from dock that arent added to dock and also have no
accompanying windows open
me immediately after doing it (even after restarting terminal)


- remove several icons from dock

- add the 'do not use timemachine' defaults flag
